# CMS API — when set, all data functions switch from mock data to real API calls
# Leave empty during development to use local mock data
NEXT_PUBLIC_API_URL=
API_URL=

# Examples:
# NEXT_PUBLIC_API_URL=https://api.yourportfolio.com
# API_URL=https://api.yourportfolio.com  (server-side only, not exposed to browser)
